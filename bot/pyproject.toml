[project]
name = "bot"
version = "0.1.0"
description = "Telegram bot for FastChat notifications"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.13.1,<4",
    "pydantic-settings>=2.6.0,<3",
]
